# Local inventory targets — never sent to unauth.dev.
# host: address or DNS name to probe
# owner / env: optional correlation tags from YOUR CMDB (scanner does not invent them)

targets:
  - host: 10.0.1.5
    owner: ml-platform
    env: production
  - host: ollama.dev.internal
    owner: data-science
    env: development
